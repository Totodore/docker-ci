version: "3"
services: 
  app:
    image: "httpd"
    container_name: httpd
    volumes: 
      - "data:/data"
    networks: 
      - "web"
    labels: 
      - "docker-ci.enable=true"
      - "docker-ci.name=hello-world"

networks: 
  web:
    external: true

volumes: 
  data: