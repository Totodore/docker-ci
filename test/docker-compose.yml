version: "3"
services: 
  app:
    image: httpd
    container_name: soundbox
    volumes: 
      - "data:/data"
    networks: 
      - "web"
    labels: 
      docker-ci.enable: 'true'
      docker-ci.name: hello-world
      docker-ci.repo-url: httpd
      docker-ci.webhook-secret: 8b0a9b6c0701f26ce953948f86c2281b
      # docker-ci.webhook-callback: 'true'

networks: 
  web:
    external: true

volumes: 
  data: