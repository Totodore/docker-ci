version: "3"
services: 
  app:
    image: httpd
    container_name: blabla
    volumes: 
      - "data:/data"
    networks: 
      - "web"
    labels: 
      docker-ci.enable: true
      # docker-ci.webhook-callback: 'true'

networks: 
  web:
    external: true

volumes: 
  data: